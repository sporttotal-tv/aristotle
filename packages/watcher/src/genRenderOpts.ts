import { RenderOpts } from './types'
import { BuildResult } from '@saulx/aristotle-build'

export default (build: BuildResult) => {
  const renderOpts: RenderOpts = {
    body: '',
    head: ''
  }
}
